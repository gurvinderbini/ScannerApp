<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScannerApp.Views.LoginPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height=".5*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image Grid.Row="0" Source="icon.png" HorizontalOptions="CenterAndExpand"
                   VerticalOptions="CenterAndExpand" Aspect="AspectFill"/>
            <StackLayout Grid.Row="1" VerticalOptions="StartAndExpand"
                         HorizontalOptions="FillAndExpand" Margin="20">
                <!--<Entry Text="{Binding Name}" Placeholder="Username"/>-->

                <Entry Text="{Binding Password}" IsPassword="True" Placeholder="Password"/>

                <Button Text="Login" Command="{Binding LoginCommand}" BackgroundColor="DodgerBlue"
                        TextColor="White" Margin="0,10"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>